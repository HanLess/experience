在java中字符串是引用数据类型，是数组的一种，又因为java是静态类型语言，所以引用类型变量声明后不可改变

在java中，相同字符串赋值给不通变量，两个变量其实指向同一处内存。

如 String a = "hello world";
  String b = "hello world";
  
  a 与 b指向同一处，而当给a重新赋值，其实是将a指向另一处
  
如果想要构造字符串，如高频率更改一个字符串，用普通方法操作会造成内存的浪费，效率低，可以使用 StringBuiler类
