// 十进制转二进制
var transform = function(num){
  var re = [] , base = num , chu , yu
  while(chu !== 0){
    chu = Math.floor(base / 2)
    re.push((base - chu * 2))
    base = chu
  }

  return re.reverse().join('')
}
