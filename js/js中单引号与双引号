一般情况下js中的单双引号是相同的，但是近期有一个项目是从url中传递字符串数组：

['ceshi']

从url中拿到之后是一个字符串，问题出现在这里。

['ceshi'] 与 ["ceshi"]是不同的

如果在url中的数据是单引号 ['ceshi'] ，那么取到之后就是 "['ceshi']" ，用 JSON.parse 去解析会报错，' 为非法字符

["ceshi"] 取到是 "["ceshi"]" ，样子比较奇怪，但它是一个字符串 ，用 JSON.parse 解析后正常返回数组 ["ceshi"]

问题原因：json数据中是没有单引号的，虽然是用在js中但要想使用json对象的方法必须遵守json标准，具体查看 MDN 文档

解决办法：eval转义可以通用两种写法

