```
  var help = function(arr,ind){
            var index = ind , count = 1
            while(count < 3){
                if(!arr[index]){
                    count ++
                    index ++
                }else{
                    index ++
                }
            }
            return index
        }

        var my = function(){
            var sort = []
            for(let i = 0;i < 13;i ++){
                sort.push(false)
            }
            var count = 1;
            var ind = 0;
            while(count <= 13){
                if(ind <= 12){
                    if(!sort[ind]){
                        sort[ind] = count
                        count ++
                        ind = help(sort,ind)
                    }else{
                        ind ++
                    }
                }else{
                    ind = 0
                }
            }
            console.log(sort)
        }
my()
```
